# =====================================================
# deploy.yml — CI/CD GitHub Actions
# Déploiement automatique sur o2switch à chaque push
# sur la branche master
# =====================================================

name: Deploy Project Manager

on:
  push:
    branches:
      - master

jobs:
  # =====================
  # JOB 1 — Build Frontend React
  # =====================
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest

    steps:
      # Checkout du code
      - name: Checkout
        uses: actions/checkout@v4

      # Installation Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          # node-version: "22"
          # cache: "npm"

      # # Installation des dépendances
      # - name: Install dependencies
      #   run: npm ci

      # Installation des dépendances
      - name: Install dependencies
        run: npm install --legacy-peer-deps --ignore-scripts

      # Build de production
      - name: Build
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_DROPBOX_APP_KEY: ${{ secrets.VITE_DROPBOX_APP_KEY }}

      # Upload des artifacts de build
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: dist/
          retention-days: 1

  # =====================
  # JOB 2 — Deploy sur o2switch via FTP
  # =====================
  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    needs: build-frontend

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: dist/

      # Déploiement FTP sur o2switch
      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: dist/
          server-dir: /public_html/project-manager/

  # =====================
  # JOB 3 — Deploy Backend Symfony sur o2switch
  # =====================

  deploy-backend:
    name: Deploy Backend
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via Webhook
        run: |
          curl --http1.1 --insecure --max-time 30 -X POST "${{ secrets.DEPLOY_WEBHOOK_URL }}" \
            -H "X-Deploy-Token: ${{ secrets.DEPLOY_TOKEN }}" || true

  # deploy-backend:
  #   name: Deploy Backend
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Setup SSH key
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
  #         chmod 600 ~/.ssh/id_rsa
  #         ssh-keyscan -p 22 ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true

  #     - name: Deploy via SSH
  #       run: |
  #         ssh -i ~/.ssh/id_rsa -p 22022 -o StrictHostKeyChecking=no \
  #           ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} \
  #           "cd ~/public_html/project-manager-api && git pull origin master && php bin/console cache:clear"

  # deploy-backend:
  #   name: Deploy Backend
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USERNAME }}
  #         password: ${{ secrets.SSH_PASSWORD }}
  #         port: 22022
  #         script: |
  #           cd ~/public_html/project-manager-api
  #           git pull origin master
  #           composer install --no-dev --optimize-autoloader
  #           php bin/console cache:clear
  #           echo "✅ Backend déployé avec succès !"
